# ============================================================
# ansible/inventory.ini — Target Hosts
# Replace IP addresses with your actual server IPs
# ============================================================

[webservers]
# Example: 20.10.5.100 ansible_user=azureuser ansible_ssh_private_key_file=~/.ssh/id_rsa
<your-server-ip> ansible_user=azureuser ansible_ssh_private_key_file=~/.ssh/id_rsa
